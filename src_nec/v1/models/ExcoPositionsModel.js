const mongoose = require('mongoose');
const Schema = mongoose.Schema;

const excoPositonSchema = new Schema({

})

const ExcoPosition = mongoose.model('ExcoPosition', excoPositonSchema);
module.exports = ExcoPosition;
